const initState = {
    isEmpty: true,
    userInfo: {}
}

const authReducer = (state = initState, { type, payload }) => {
    switch(type){
        default: 
            return state;
    }
}

export default authReducer;